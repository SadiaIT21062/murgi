#include<stdio.h>
int recursion(int a,int b,int c,int n)
{
    int i,j;
    for(i=4;i<=n;i++)
    {
        sum=0;
        for(j=i-1;j>=i-3;j--)
        {
            sum=sum+s[j];
        }
        s[i]=sum;
    }
}
int main()
{
    int n,a,b,c;
    scanf("%d",&n);
    scanf("%d%d%d",&a,&b,&c);

}

